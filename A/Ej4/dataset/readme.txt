This folder stores all files that filter.out has generated from another files.

In other words, contains all files tat filter.out has outputted.

It also contains all files that permgen.out has generated from another files.

In other words, contains all files tat permgen.out has outputted.
